type Market @entity {
  id: ID!
  marketId: Bytes!        # Market ID stored as Bytes
  name: Bytes!            # Human-readable name also stored as Bytes
  conditionId: Bytes!     # Condition ID stored as Bytes
}
